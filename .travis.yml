os: linux

# safelist
branches:
  only:
    - develop
    - master

language: python
  - "3.6"
  - "3.7"
  - "3.8"

# install dependencies
install:
  - pip install -r requirements38.txt

script:
  - pytest
